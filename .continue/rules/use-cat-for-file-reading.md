---
alwaysApply: true
---

When asked to read or display file contents, always use the `cat` command via `read` with `cat` command. Do not use `read_file` tool or any other file reading mechanism. Use `cat` for all file reading operations.