#usda 1.0
(
    doc ="""Generated from node description file OgnFlipFlop.ogn
Contains templates for node types found in that file."""
)

def OmniGraph "TestGraph"
{
    token evaluator:type = "push"
    int2 fileFormatVersion = (1, 3)
    token flatCacheBacking = "Shared"
    token pipelineStage = "pipelineStageSimulation"

    def OmniGraphNode "Template_omni_graph_action_FlipFlop" (
        docs="""This node activates its outputs in an alternating sequence, starting with 'On Odd' on the first execution after 'Signal Execution' has been activated."""
    )
    {
        token node:type = "omni.graph.action.FlipFlop"
        int node:typeVersion = 2

        # 1 attribute
        custom uint inputs:execIn (
            docs="""Signal to the graph that this node is ready to be executed."""
        )

        # 3 attributes
        custom uint outputs:a (
            docs="""After every odd-numbered execution signal to the graph that execution can continue downstream."""
        )
        custom uint outputs:b (
            docs="""After every even-numbered execution signal to the graph that execution can continue downstream."""
        )
        custom bool outputs:isA (
            docs="""Set to true when the 'Execute A' signal is active, otherwise set to false."""
        )
    }
}
