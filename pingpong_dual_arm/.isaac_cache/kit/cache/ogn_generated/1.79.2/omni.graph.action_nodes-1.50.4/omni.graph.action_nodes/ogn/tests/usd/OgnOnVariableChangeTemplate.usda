#usda 1.0
(
    doc ="""Generated from node description file OgnOnVariableChange.ogn
Contains templates for node types found in that file."""
)

def OmniGraph "TestGraph"
{
    token evaluator:type = "push"
    int2 fileFormatVersion = (1, 3)
    token flatCacheBacking = "Shared"
    token pipelineStage = "pipelineStageSimulation"

    def OmniGraphNode "Template_omni_graph_action_OnVariableChange" (
        docs="""Activates execution of the downstream graph when a graph variable's value changes. The name of the variable comes from the input 'Variable Name'. Only variables in the current graph are considered."""
    )
    {
        token node:type = "omni.graph.action.OnVariableChange"
        int node:typeVersion = 2

        # 2 attributes
        custom bool inputs:onlyPlayback = true (
            docs="""When true, the node is only executed while the Stage is being played."""
        )
        custom token inputs:variableName = "" (
            docs="""The name of the graph variable to monitor for changes."""
        )

        # 1 attribute
        custom uint outputs:changed (
            docs="""When the variable value changes, signal to the graph that execution can continue downstream."""
        )
    }
}
