#usda 1.0
(
    doc ="""Generated from node description file OgnOnMessageBusEvent.ogn
Contains templates for node types found in that file."""
)

def OmniGraph "TestGraph"
{
    token evaluator:type = "push"
    int2 fileFormatVersion = (1, 3)
    token flatCacheBacking = "Shared"
    token pipelineStage = "pipelineStageSimulation"

    def OmniGraphNode "Template_omni_graph_action_OnMessageBusEvent" (
        docs="""Event node which fires when the specified event appears on the Application Message Bus. The node uses a callback to monitor messages on the message bus and sets an internal state value when an event named 'Event Name' was received."""
    )
    {
        token node:type = "omni.graph.action.OnMessageBusEvent"
        int node:typeVersion = 2

        # 2 attributes
        custom token inputs:eventName = "" (
            docs="""The name of the custom event."""
        )
        custom bool inputs:onlyPlayback = true (
            docs="""When true, the node is only executed while the Stage is being played."""
        )

        # 1 attribute
        custom uint outputs:execOut (
            docs="""After receipt of the named message on the application message bus
            signal to the graph that the message bus event was received and execution can continue downstream."""
        )
    }
}
