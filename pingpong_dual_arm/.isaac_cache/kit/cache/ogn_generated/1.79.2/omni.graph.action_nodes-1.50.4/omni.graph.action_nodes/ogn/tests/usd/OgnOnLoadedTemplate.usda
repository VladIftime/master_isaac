#usda 1.0
(
    doc ="""Generated from node description file OgnOnLoaded.ogn
Contains templates for node types found in that file."""
)

def OmniGraph "TestGraph"
{
    token evaluator:type = "push"
    int2 fileFormatVersion = (1, 3)
    token flatCacheBacking = "Shared"
    token pipelineStage = "pipelineStageSimulation"

    def OmniGraphNode "Template_omni_graph_action_OnLoaded" (
        docs="""Activates the output on the first update of the graph after it is created or loaded. This will run before any other event node, and will only run once after this node is created."""
    )
    {
        token node:type = "omni.graph.action.OnLoaded"
        int node:typeVersion = 2

        # 1 attribute
        custom uint outputs:execOut (
            docs="""On first create or load of the graph signal to the graph that execution can continue downstream."""
        )
    }
}
